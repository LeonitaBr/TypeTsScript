import React, {memo} from 'react';
import {View} from 'react-native';
import LocationService from '../services/LocationService';

const WeatherScreen = () => {
  return <View testID="weather-screen"></View>;
};
export default memo(WeatherScreen);
