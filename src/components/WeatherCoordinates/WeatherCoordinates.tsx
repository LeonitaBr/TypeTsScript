import React, {memo} from 'react';
import {Text, View} from 'react-native';

const WeatherCoordinates = () => {
  return (
    <View testID="weather-coordinates">
      <Text>1111</Text>
    </View>
  );
};
export default memo(WeatherCoordinates);
